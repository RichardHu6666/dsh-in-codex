# 安全说明

本项目适合可信本机开发环境，不是多租户执行平台或操作系统沙箱。
目录检查限制任务入口，不能保证隔离 Shell、子进程、网络或其他可读文件。

## 部署建议

- 不可信任务使用专用账户、虚拟机或经验证的容器隔离。
- 不向公共网络暴露服务，不将生产凭证放进任务环境。
- API Key 使用环境变量或本地 `.env`，不提交到 Git。
- `.runtime`、SDK 历史、测试报告可能包含敏感内容，分享前逐项脱敏。
- 取消仅停止已识别的进程，不回滚修改，不保证控制恶意脱离进程树的程序。
- Windows 与 WSL 不要共享活动运行状态目录。

## 报告漏洞

优先使用仓库 Security 页的私密漏洞报告功能（若已启用）。
如果该入口不可用，可创建不含漏洞利用细节、凭证或敏感数据的 Issue，
请求维护者提供私密沟通方式。不要公开上传 `.env` 或完整 `.runtime`。

报告中请提供版本、平台、最小复现和预期影响，不要包含真实密钥。
若密钥已经泄露，应先撤销或轮换密钥，再处理历史记录；删除文件本身不能撤销泄露。
